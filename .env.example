GOOGLE_API_KEY=your_google_ai_studio_key_here

# Optional: used by `docker compose run --rm eval` to pick a default eval
# run id (outputs/eval_runs/<run_id>/) without typing RUN_ID=... every time.
# Not required for local (non-Docker) runs, where run_id is a CLI argument
# to scripts/run_eval_extractions.py / scripts/score_eval.py instead.
# RUN_ID=default